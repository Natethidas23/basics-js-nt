// Template literals 
let firstName = 'Nate T'
const homeTown = 'South Florida'
console.log(`Yolo my name is ${firstName}. I'm from ${homeTown}.`)

// Combine Ternary and Template Literal
let str = `Yolo how are you doing ${{firstName} ? firstName: 'No Name'}`
console.log(str)
